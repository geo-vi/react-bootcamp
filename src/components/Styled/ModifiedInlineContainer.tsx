import styled from "styled-components";
import InlineContainer from "./InlineContainer";

const ModifiedInlineContainer = styled(InlineContainer)`
  margin-top: 10px;
  width: 400px;
  justify-content: space-around;
`

export default ModifiedInlineContainer;
